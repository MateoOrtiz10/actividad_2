import 'package:flutter/material.dart';

class ImageMusic extends StatelessWidget {
  const ImageMusic({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
        children: [
          Image.asset('assets/images/Imagen4.png')
        ],
    );
  }
}